1. Files in this folder should be placed under the root folder.
   They enable running the tests on a clean start of a machine.

2. The file testjob.conf should be copied to /etc/init.
   It is used by upstart upon a clean start of the machine to trigger the tests.

